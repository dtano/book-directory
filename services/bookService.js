const {Author, Book} = require('../models/');
const {isEmpty, isNullOrEmpty, deleteFile} = require('../controllers/general');

const bookService = {
    createBook: async (details) => {
        
    }
};

module.exports = bookService;

